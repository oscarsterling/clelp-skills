# secret-values.example.txt
#
# The plaintext list of the EXACT secret values that must never egress. You
# build a salted-hash index from this file (see SKILL.md), and the running gate
# matches against the index, never against this file. This file is only read at
# build time.
#
# CRITICAL: the real version of this file is a local secret. NEVER commit it,
# push it, or leave it readable by the scanned agent. Add its path to your
# ignore rules and keep it 0600. Populate it in a build step from your own
# vault, environment, or secret manager. This example ships only placeholders.
#
# Format: one secret per line. Blank lines and #-comments are ignored. Lines
# shorter than 8 characters are skipped (too short to match without noise).
#
# Replace the placeholders below with your real values in your local copy:
replace-with-a-real-secret-value-0001
replace-with-a-real-secret-value-0002
